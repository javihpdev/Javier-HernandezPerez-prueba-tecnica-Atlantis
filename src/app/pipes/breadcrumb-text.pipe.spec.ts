import { BreadcrumbTextPipe } from './breadcrumb-text.pipe';

describe('BreadcrumbTextPipe', () => {
  it('create an instance', () => {
    const pipe = new BreadcrumbTextPipe();
    expect(pipe).toBeTruthy();
  });
});
